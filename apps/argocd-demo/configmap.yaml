apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-page
  namespace: argocd-demo
data:
  index.html: |
    <!doctype html>
    <html lang="en">
      <head><meta charset="utf-8"><title>Argo CD GitOps Demo</title></head>
      <body style="font-family: sans-serif; max-width: 48rem; margin: 4rem auto; background: #6b2d5c; color: white;">
        <h1>Argo CD synchronized this application</h1>
        <p>Version: v2</p>
        <p>Use the Argo CD resource tree to compare desired and live state.</p>
      </body>
    </html>
