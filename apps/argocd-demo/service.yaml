apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: argocd-demo
spec:
  selector:
    app.kubernetes.io/name: argocd-demo-web
  ports:
    - name: http
      port: 80
      targetPort: http
