apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: flux-demo
spec:
  selector:
    app.kubernetes.io/name: flux-demo-web
  ports:
    - name: http
      port: 80
      targetPort: http
